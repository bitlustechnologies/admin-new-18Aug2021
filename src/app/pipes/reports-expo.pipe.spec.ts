import { ReportsExpoPipe } from './reports-expo.pipe';

describe('ReportsExpoPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportsExpoPipe();
    expect(pipe).toBeTruthy();
  });
});
