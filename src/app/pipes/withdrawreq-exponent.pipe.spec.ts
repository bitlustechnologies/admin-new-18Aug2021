import { WithdrawreqExponentPipe } from './withdrawreq-exponent.pipe';

describe('WithdrawreqExponentPipe', () => {
  it('create an instance', () => {
    const pipe = new WithdrawreqExponentPipe();
    expect(pipe).toBeTruthy();
  });
});
