import { OrderExponentPipe } from './order-exponent.pipe';

describe('OrderExponentPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderExponentPipe();
    expect(pipe).toBeTruthy();
  });
});
