import { BankdepositExponentPipe } from './bankdeposit-exponent.pipe';

describe('BankdepositExponentPipe', () => {
  it('create an instance', () => {
    const pipe = new BankdepositExponentPipe();
    expect(pipe).toBeTruthy();
  });
});
