import { CurrencyExponentPipe } from './currency-exponent.pipe';

describe('CurrencyExponentPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyExponentPipe();
    expect(pipe).toBeTruthy();
  });
});
