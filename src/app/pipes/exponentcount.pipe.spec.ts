import { ExponentcountPipe } from './exponentcount.pipe';

describe('ExponentcountPipe', () => {
  it('create an instance', () => {
    const pipe = new ExponentcountPipe();
    expect(pipe).toBeTruthy();
  });
});
