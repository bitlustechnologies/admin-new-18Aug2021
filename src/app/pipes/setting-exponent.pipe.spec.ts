import { SettingExponentPipe } from './setting-exponent.pipe';

describe('SettingExponentPipe', () => {
  it('create an instance', () => {
    const pipe = new SettingExponentPipe();
    expect(pipe).toBeTruthy();
  });
});
