import { FivedecimaldirectiveDirective } from './fivedecimaldirective.directive';

describe('FivedecimaldirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FivedecimaldirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
